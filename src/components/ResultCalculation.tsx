import React, { useContext } from "react";
// import { places } from "../data";
// import { Cuisine, District, PlaceType } from "../types";

const ResultCalculation: React.FC = () => {

    // const { chosenPlaceType, setChosenPlaceType } = useContext(PlaceTypeContext)
    
    return <span>hello</span>

    // return <span>{chosenPlaceType}</span>

    // const filteredByPlaceType = (value: PlaceType) => {
    //     return places.filter(place => place.category.includes(value))
    // };

    // const filteredByDistrict = (value: District) => {
    //     return places.filter(place => place.district.includes(value))
    // };

    // const filteredByCuisine = (value: Cuisine) => {
    //     return places.filter(place => place.cuisine?.includes(value))
    // };

}


export default ResultCalculation;
